# دليل بناء وتشغيل المتصفح عبر GitHub Actions 🚀

تم إعداد وتجهيز مستودع **Nora Browser** بنظام بناء آلي احترافي ومتكامل عبر **GitHub Actions**، ينتج حزم تثبيت منفصلة لكل معمارية معالج (ABI Splits) بالإضافة للحزمة الشاملة (Universal).

---

## 📁 الملفات التي تم إنشاؤها وتجهيزها:
1. **`.github/workflows/android-build.yml`**: ملف سير العمل (Workflow) الرئيسي للبناء والاختبار واستخراج الـ APKs.
2. **`gradlew` و `gradlew.bat`**: مشغّل Gradle مع الصلاحيات التنفيذية المطلوبة لبيئة لينكس.
3. **`gradle/wrapper/gradle-wrapper.jar`**: حزمة Gradle Wrapper الرسمية المتوافقة مع إصدار Gradle 9.3.1.
4. **`debug.keystore.base64`**: مفتاح التوقيع المشفر لاستعادته آلياً داخل خوادم GitHub Actions بدون الحاجة لرفع مفتاح غير آمن إلى المستودع العام.

---

## 📱 حزم الـ APK الناتجة لكل معمارية (Split APKs):
عند اكتمال البناء في GitHub Actions، ستجد في قسم **Artifacts** أو في **Releases**:

| اسم الملف / المعمارية | الأجهزة المستهدفة | الحجم والفائدة |
| :--- | :--- | :--- |
| **`app-arm64-v8a-*.apk`** | غالبية هواتف وأجهزة أندرويد الحديثة (64-bit ARM) | حجم خفيف جداً وأداء فائق مخصص لمعالجات الهواتف الحديثة |
| **`app-armeabi-v7a-*.apk`** | هواتف أندرويد القديمة أو الاقتصادية (32-bit ARM) | توافق كامل مع الأجهزة الأقدم |
| **`app-x86_64-*.apk`** | محاكيات أندرويد الحديثة على الكمبيوتر وأجهزة ChromeOS | تشغيل مباشر على أجهزة x86_64 بدون طبقة ترجمة |
| **`app-x86-*.apk`** | المحاكيات القديمة (32-bit x86) | توافق المحاكيات |
| **`app-universal-*.apk`** | حزمة شاملة (Universal) تضم كافة المعماريات | تعمل على أي جهاز أندرويد ولكن بحجم أكبر |
| **`SHA256SUMS.txt`** | ملف البصمة الرقمية والتحقق | للتحقق من سلامة وصحة الملفات ومطابقتها |

---

## 🛡️ أخطاء GitHub Actions الشائعة التي تم تفاديها وحلها مسبقاً:

1. **خطأ انعدام ملف الـ Wrapper (`bash: ./gradlew: No such file or directory`)**:
   - **السبب**: المستودع لم يكن يحتوي على `gradlew` في المجلد الرئيسي.
   - **الحل**: تم توليد ملفات `gradlew` و `gradlew.bat` و `gradle-wrapper.jar` مع إضافة أمر `chmod +x ./gradlew` تلقائياً في أول خطوة.

2. **خطأ عدم توافق إصدار جافا (Java Version Mismatch)**:
   - **السبب**: إصدار Gradle هو 9.3.1 ويتطلب Java 17 أو Java 21 على الأقل.
   - **الحل**: تم تحديد JDK 21 (Temurin) في سير العمل لضمان التوافق الأقصى مع Kotlin 2.1 و Target SDK 36.

3. **خطأ ملحق الأسرار ومفاتيح البيئة (`Secrets Gradle Plugin / .env`)**:
   - **السبب**: فشل البناء إذا لم يجد الملحق ملف `.env`.
   - **الحل**: يقوم سير العمل بفحص وجود `.env`، وفي حال عدم وجوده يستنسخه تلقائياً من `.env.example` أو ينشئ ملفاً فارغاً لضمان عدم توقف البناء.

4. **خطأ تراخيص حزمة تطوير أندرويد (SDK Licenses Not Accepted)**:
   - **السبب**: توقف البناء بانتظار الموافقة على تراخيص Google/Android SDK.
   - **الحل**: تم تضمين خطوة الموافقة التلقائية على جميع التراخيص (`yes | sdkmanager --licenses || true`).

5. **خطأ مفاتيح التوقيع (`Keystore Not Found / Password Missing`)**:
   - **السبب**: يفشل بناء Release أو Debug إذا كانت مسارات الـ Keystore مفقودة.
   - **الحل**:
     - يتم فك تشفير `debug.keystore.base64` تلقائياً لضمان توقيع حزم Debug وجعلها قابلة للتثبيت الفوري على أي هاتف بدون قيود.
     - في حال لم تضف مفاتيح Release مخصصة، يقوم السير بعمل Fallback للتوقيع بمفتاح الـ Debug حتى لا يفشل البناء، وتظل الحزم جاهزة للتجربة والتثبيت.
     - إذا أردت استخدام مفتاحك الخاص للإنتاج، ما عليك سوى إضافة Secret في GitHub باسم `KEYSTORE_BASE64` و `KEYSTORE_PASSWORD` و `KEY_PASSWORD`.

---

## 🕹️ كيفية تشغيل البناء يدوياً (Manual Run):
1. اذهب إلى تبويب **Actions** في مستودعك على GitHub.
2. اختر من القائمة اليسرى **"Android CI & Split APKs Build"**.
3. اضغط على زر **"Run workflow"**.
4. اختر نوع البناء المطلوب:
   - `debug`: بناء حزم الاختبار الموقعة (موصى بها للتجربة المباشرة).
   - `release`: بناء حزم الإنتاج.
   - `both`: بناء الاثنين معاً.
5. حدد إذا كنت ترغب في تشغيل اختبارات الوحدة أو إنشاء GitHub Release تلقائياً.
6. اضغط **"Run workflow"** وانتظر دقائق معدودة، ثم حمّل ملفاتك من قسم **Artifacts** أسفل صفحة التشغيل.

---

## 🏷️ النشر التلقائي عبر الـ Tags:
عند إنشاء Tag جديد من نوع `v*` (مثل `git tag v1.0.0 && git push --tags`):
- سيقوم GitHub Actions ببناء كل المعماريات تلقائياً.
- سيتم إنشاء **Release** رسمي على GitHub ورفع جميع ملفات الـ APK مع ملف التحقق `SHA256SUMS.txt` داخله مباشرة.
